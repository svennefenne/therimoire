// Superseded by useAudiobookHotkeys, which does everything this used to
// (±15s skip) plus volume and play/pause, all with on-screen feedback. Kept
// as a re-export so nothing importing this path breaks.
export { default } from './useAudiobookHotkeys'
